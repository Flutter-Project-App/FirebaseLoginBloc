name: authentication_repository
description: Dart
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cache:
    path: ../cache
  equatable:
  firebase_auth:
  firebase_core:
  google_sign_in:
  meta:
  very_good_analysis:

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: